# NAOMA DIAL - Dependencies
# Pinned versions for Railway stability

# FastMCP - MCP Server Framework
fastmcp>=2.0.0,<3.0.0

# FastAPI - Web Framework
fastapi>=0.109.0,<1.0.0

# Uvicorn - ASGI Server
uvicorn[standard]>=0.27.0,<1.0.0

# Starlette (comes with FastAPI but pinned for stability)
starlette>=0.35.0,<1.0.0

# Pydantic (comes with FastAPI)
pydantic>=2.0.0,<3.0.0

# HTTP client
httpx>=0.26.0,<1.0.0

# WebSockets (pinned to avoid deprecation warnings)
websockets>=12.0,<14.0
